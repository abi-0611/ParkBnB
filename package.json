{
  "name": "parknear",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev:mobile": "pnpm --filter mobile start",
    "dev:web": "pnpm --filter web dev",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "deploy:web:build": "pnpm --filter web build",
    "deploy:mobile:android:preview": "pnpm --filter mobile exec eas build --platform android --profile preview",
    "supabase:db:push": "supabase db push",
    "supabase:functions:deploy": "supabase functions deploy create-razorpay-order verify-payment cancel-booking detect-no-show send-notification"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0"
  }
}
